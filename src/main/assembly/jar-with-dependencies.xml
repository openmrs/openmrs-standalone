<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3 http://maven.apache.org/xsd/assembly-1.1.3.xsd">
    <!--
      Equivalent to the built-in `jar-with-dependencies` descriptor, but with the
      metaInf-services container handler enabled so that META-INF/services/* files
      from every dependency are CONCATENATED rather than overwritten (last-jar-wins).

      Without this, the executable standalone jar ended up with a
      META-INF/services/java.sql.Driver containing only mysql-connector's
      com.mysql.cj.jdbc.Driver - mariadb-java-client's org.mariadb.jdbc.Driver entry
      was silently dropped, so the MariaDB driver was never auto-registered via the
      JDBC ServiceLoader. That surfaced as "No suitable driver found for jdbc:mariadb://"
      on any jdbc:mariadb connection that wasn't preceded by an explicit Class.forName.
    -->
    <id>jar-with-dependencies</id>
    <formats>
        <format>jar</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>
    <containerDescriptorHandlers>
        <containerDescriptorHandler>
            <handlerName>metaInf-services</handlerName>
        </containerDescriptorHandler>
    </containerDescriptorHandlers>
    <dependencySets>
        <dependencySet>
            <outputDirectory>/</outputDirectory>
            <useProjectArtifact>true</useProjectArtifact>
            <unpack>true</unpack>
            <scope>runtime</scope>
        </dependencySet>
    </dependencySets>
</assembly>
